ActiveMQ: 稳定性没有RabbitMQ高，吞吐量没有Kafka高；只是对JMS支持比较好，且消息队列、主题订阅两种模式分别类似kafka的单一partition和
          partition的主从模式。队列不支持多用户，主题订阅每次都发给所有用户。
Kafka: 同一个partition是顺序的，但分区之间不是有序的
RabbitMQ: 是erlang写的，多用于金融系统，稳定性高 

对比总结:
  # ---------------------------------------------------------AI的总结------------------------------------------------------------
  # 特性          Kafka                RabbitMQ                RocketMQ             ActiveMQ      #    NATS           Pulsar
  # ----------------------------------------------------------------------------------------------------------------------------
  # 吞吐量        非常高                  中等                     高                   中等         #     高              非常高
  # 扩展性        非常好                  有限                     好                    有限        #     好              非常好
  # 复杂性        高                      低                      中等                  中等        #     低               高
  # ----------------------------------------------------------------------------------------------------------------------------
  # 延迟           低                     低                      低                   中等         #    非常低            低
  # 持久化         支持                   支持                     支持                  支持        #    不支持           支持
  # 事务支持      有限支持                 支持                     支持                  支持        #    不支持            支持
  # ----------------------------------------------------------------------------------------------------------------------------
  # 适用场景  大数据、流处理          企业应用、微服务          大规模分布式系统            企业应用      #微服务、实时通信  大规模分布式系统
  # 社区活跃     很活跃                   活跃         阿里的、社区小、文档不如前两者丰富   一般、支持多种协议
  # 缺点  配置复杂实时性比RabbitMQ低   配置复杂扩展性有限       配置复杂学习曲线较高   性能低配置复杂扩展性有限
  # ----------------------------------------------------------------------------------------------------------------------------
  # 结论: （吞吐量、配置复杂度、持久化、性能、实施性、扩展性、社区活跃度）
  #   Kafka 适合大数据和流处理场景，具有高吞吐量和持久化存储的优势。配置复杂、依赖zk、实时性不如RabbitMQ
  #   RabbitMQ 适合需要灵活消息模式的企业应用，易于使用和管理。吞吐量较低不适合大数据场景、配置复杂、扩展性有限
  #   RocketMQ 适合大规模分布式系统，支持高吞吐量和分布式事务。配置复杂、学习曲线较高
  #   ActiveMQ 适合需要JMS兼容的企业应用，但性能相对较低。性能较低、吞吐量较低、配置复杂、扩展有限
  #   1: 从社区活跃度、吞吐量、性能这几个维度就剔除了RocketMQ和AcitveMQ。
  #   2: 如果是高吞吐量、高性能的可以使用kafka，可能需要类似jd那样开发一套管理后台（kafka快的原因：IO使用Segment顺序存储、零拷贝、broker和partition提高并发度）
  #   3: 如果是对大数据、性能、扩展性要求不高的，可以使用RabbitMQ
                            ---------------------------------------------------------------------------
                            特性         ActiveMQ     RabbitMQ        RocketMQ            Kafka
                            ---------------------------------------------------------------------------
                            单机吞吐量      万级         万级            10 万级            10 万级
                            支持主题数      千级        百万级            千级          百级，多了性能严重下滑
                            时效性        毫秒级      （微秒级）          毫秒级             毫秒级
                            可用性       高（主从）    高（主从）       非常高（分布式）     非常高（分布式）
                            消息重复     至少一次      至少一次        至少一次 最多一次    至少一次最多一次
                            消息顺序性      有序        有序              有序              分区有序
                            消息回溯      不支持       不支持         支持（按时间回溯）   支持（按offset回溯）
                            管理界面       普通         普通              完善                普通
                            ---------------------------------------------------------------------------
                            擅长领域      功能复杂      低延迟            高性能              高性能
                            ---------------------------------------------------------------------------
                                                # 感觉阿里的RocketMQ继承了kafka的优点 # 
                                                #     RabbitMQ以微秒的时效作为招牌    # 
                            # 以上分析缺少了事务、持久化方面的对比。activemq支持多种协议JMS、amqp、mqtt等和消息
                            # 优先级、调度、重试机制。rabbitmq专注低延迟消息传递支持灵活的路由机制，所以这两个慢。
                            ---------------------------------------------------------------------------
  选型的时候，我们需要根据业务场景，结合上述特性来进行选型:
  1.比如你要支持天猫双十一类超大型的秒杀活动，这种一锤子买卖，那管理界面、消息回溯啥的不重要。我们需要看什么？看吞吐量！所以优先选Kafka和RocketMQ这种更高吞吐的。
  2.比如做一个公司的中台，对外提供能力，可能会有很多主题接入，这时主题个数是很重要的考量，像Kafka这样百级的就不太符合要求，可根据情况考虑千级的RocketMQ甚至百万级的RabbitMQ。
  3.又比如是一个金融类业务，那么重点考虑的就是稳定性、安全性，分布式部署的Kafka和Rocket就更有优势。(持久性和可靠性，activemq和rabbtimq的专注点不一样)
  4.特别说一下时效性，RabbitMQ以微秒的时效作为招牌，但实际上毫秒和微秒在绝大多数情况下都没有感知的区别，加上网络带来的波动，这一点在生产过程中，反而不会作为重要的考量。







